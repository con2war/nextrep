/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure CSS optimization isn't disabled for production
  optimizeCss: true,
  // Add other necessary config
}

module.exports = nextConfig 